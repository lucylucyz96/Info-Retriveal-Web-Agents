To retreive top 20 documents, remove the comment for top_20_docs(). To retreive relevant terms for top 10 documents, remove the comment for top_10(). To retreive most similar 20 documents to doc 239,1236,2740, remove the comment for most_similar(). To enter queries through keyboard, remove the comment for top_20_docs_from_input(). To use my dataset, replace "cacm.raw" with news1.raw, replace "query.raw" with "news_query.raw" and replace "query.rels" with "news_query.rels".